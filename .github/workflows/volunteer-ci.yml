name: CI/CD DevSecOps - Volunteer Service

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  security-and-build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Configurar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
        cache-dependency-path: 'volunteer-service/package-lock.json'

    - name: Executar Scan de Segurança de Código (Trivy SCA)
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: 'volunteer-service'
        format: 'table'
        exit-code: '0'
        severity: 'CRITICAL,HIGH'

    - name: Instalar Dependências e Testar
      run: |
        cd volunteer-service
        npm ci
        npm test --if-present

    - name: Simular Build da Imagem Docker Otimizada
      run: |
        cd volunteer-service
        docker build -t solidary-tech/volunteer-service:latest .
